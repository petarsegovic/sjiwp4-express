<div class="p-2">
    <div class="text-center">
            <h1 class="text-5xl font-bold">{{result.competitionName}}</h1>
    </div>
    <div class="overflow-x-auto">
        <table class="table">
            <!-- head -->

            
            <thead>
                <tr>
                    <th>Redni broj</th>
                    <th>Ime</th>
                    <th>Bodovi</th>
                </tr>
            </thead>
            <!-- body -->
            <tbody>
                    {{#each result.items}}
                <tr>
                    <td>{{@index }}</td>
                    <td>{{this.korisnik}}</td>
                    <td>{{this.bodovi}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
</div>